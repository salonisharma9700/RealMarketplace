// const fs = require('fs');

// Read the image file
// const image = fs.readFileSync('path_to_your_image.jpg');

// Encode the image to Base64
// const base64String = Buffer.from(image).toString('base64');

// Now you can use the base64String to include the image data in your JSON object
